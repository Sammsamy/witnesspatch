# 2:38 founder-voice judge demo

## Current status

The product flow is locally verified and recordable. The final public YouTube video required by the [Official Rules](https://openai.devpost.com/rules) has not been recorded or uploaded. Record a real one-take product run targeting `2:35–2:40`, never `3:00`; the controlling rule says the video must be shorter than three minutes.

Do not say the brother is a teammate until he has joined the Devpost draft and contributed. The founder may truthfully say, “I’m a third-year medical student”; that explains motivation, not clinical authority.

## Cut-ready script

| Time | Exact narration | Screen action and proof |
|---|---|---|
| `0:00–0:12` | “I’m a third-year medical student. Healthcare-agent failures often die as screenshots. WitnessPatch locks when each synthetic fact appears, then emits a standard red test and evidence chain.” | Start at `#top`. Keep `50`, `2` critical breaches, `T+02`, and **Locked rules** visible. |
| `0:12–0:28` | “At T-plus-two, the agent knows this patient is eight days postpartum, with unrelieved headache and visual changes, but waits for blood pressure arriving at T-plus-six. Locked code records two critical breaches.” | Point from `T+02` to the failed response and locked verifier. Do not scroll yet. |
| `0:28–0:45` | “I chose the synthetic fixture, narrow contract, and review gate. Codex built and attacked the post-start compiler and verifier. A fresh Codex run requested GPT-5.6 Sol with Ultra reasoning to propose declarative repair data. Neither model controls the grade.” | Point to **GPT-5.6 Sol requested**, **Codex · Ultra requested configuration**, then **Locked rules—not model-graded**. |
| `0:45–0:58` | “Compile Failure hashes and validates the two exact inputs, regrades the recorded baseline, and generates a nine-file Node test bundle locally, with no model or target rerun.” | Select **Compile failure**. Wait until the button becomes **Verify retained repair**, then use **How it works** to move to the artifact panel. |
| `0:58–1:12` | “For encoded rule INV-02, the static witness reduces nine visible facts to three at T-plus-two. The exported baseline bundle remains red; the retained repair is verified separately.” | Show `2/2 exact inputs`, `9 files`, `INV-02`, `T+02`, `9→3`, generated `regression.test.mjs`, and **BASELINE RED**. Select **Export complete 9-file ZIP** and let its label change. |
| `1:12–1:24` | “Now the separate verifier checks 23 exact artifact hashes, recomputes two reference grades, and runs four declared holdouts.” | Use the **WitnessPatch** brand to return to `#top`; select **Verify retained repair**. Wait for **Freshly verified in this browser**. |
| `1:24–1:41` | “Same case, same locked grader: 50 becomes 100, with zero critical breaches. This verified behavior-diff excerpt changes the target policy, while the exact-fact control rejects an always-escalate mutant.” | Hold on `100`, `0`, and **Contract satisfied**. Use **How it works**; show **Repair diff**, `4/4`, urgent trace, and exact-fact control. |
| `1:41–2:05` | “The retained V2 reference is separate from the fresh Sol candidate. The reference passes four of four holdouts. The Sol proposal stays quarantined and uninstalled. This browser interprets its declarative JSON; only the Node verifier executes candidate JavaScript.” | Keep the repair proof visible for the first sentence, then select **Audit receipt**. Point to **quarantined · not installed** and **Node only · browser interprets JSON IR**. |
| `2:05–2:23` | “The receipt shows 23 of 23 hashes, two of two regrades, and four of four candidate mutation checks. It records the requested model configuration, not independently attested served identity. The disclosed pre-start V1 candidate remains unchanged and fails V2.” | Show `23/23`, fresh `2/2`, fresh `4/4`, requested configuration, and `V1 85/100 fail · not executable`. |
| `2:23–2:38` | “No patient data, API key, or runtime model call. Both displayed blood-pressure classes are fixture-supplied. Physician validation remains pending; clinical use is prohibited. WitnessPatch makes failures reproducible—it does not certify safety.” | End on the receipt with **None · fully synthetic**, **API key Not required**, BP scope, **Physician validation pending**, and **Clinical use Not permitted** visible. |

## Recording preflight

- Load the page and wait at least five seconds before recording so hydration is complete.
- Record around `1400` CSS pixels wide at 1440p or higher, with clear English founder narration.
- Start from a fresh baseline. Do not precompile, splice in success, or begin on a generated artifact.
- Hide bookmarks, notifications, credentials, local paths, unrelated tabs, and private Codex traces.
- Use the **How it works** and **WitnessPatch** anchors for controlled navigation instead of free scrolling.
- Capture the real verification transitions and wait for their visible completion states.
- Keep the exported baseline explicitly **BASELINE RED** after repair verification; only the separate retained repair passes.
- Keep the uploaded cut below three minutes after YouTube processing, and add accurate captions.
- Upload `submission/video/witnesspatch-demo.en.srt`; the submission gate verifies every cue, millisecond boundary, readability limit, and word against this exact narration.
- Use no unlicensed music, third-party logos, reviewer identity, or patient information.

## Claims that are prohibited

- Do not say “Sol fixed it” or “the AI repair passed.” The retained reference and quarantined Sol candidate are distinct.
- Do not say tamper-proof, signed, publisher-authenticated, or independently attested model identity.
- Do not claim clinical validity, medical correctness, safety certification, production readiness, HIPAA compliance, lives saved, or patient outcomes.
- Do not say the browser executes candidate JavaScript or applies the patch.
- Do not imply blood-pressure threshold inference beyond the two exact fixture-supplied endpoints.
- Do not say the baseline regression turns green. It remains **BASELINE RED** beside **RETAINED REPAIR PASS**.
- Do not call the displayed patch a complete diff; it is an abridged verified behavior-diff excerpt.
- Do not call the static witness target-in-loop, counterfactual, clinically minimal, or proof of generalization.
